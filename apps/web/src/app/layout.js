export const metadata = {
  title: 'MenuBoxGh - Digital Menu & QR Ordering',
  description: 'Digital menus, QR codes, and WhatsApp ordering for food vendors in Ghana',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
